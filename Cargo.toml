[package]
name = "zfs-dedup"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Offline block-level deduplication for ZFS via FICLONERANGE"

[dependencies]

[profile.release]
lto = "thin"
codegen-units = 1
