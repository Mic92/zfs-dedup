[package]
name = "zfs-dedup"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Offline block-level deduplication for ZFS via FICLONERANGE"

[dependencies]
rustix = { version = "1", features = ["fs"] }
